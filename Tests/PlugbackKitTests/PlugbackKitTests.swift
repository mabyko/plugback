import XCTest
@testable import PlugbackKit

// ponytail: 툴체인 배선 확인용. 첫 실제 타입이 생기면 교체한다.
final class PlugbackKitTests: XCTestCase {
    func testToolchainWiring() {
        XCTAssertTrue(true)
    }
}
