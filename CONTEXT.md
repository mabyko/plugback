# Plugback

외장 화면을 연결했을 때, 그 화면 조합(작업 환경)에 저장한 대상 앱의 창들을 각각 원래 자리로 복원하는 macOS 메뉴바 유틸리티.
내장 화면의 창과 제외한 앱의 창은 다루지 않는다.

## Language

### 화면

**외장 화면** (external screen):
케이블로 맥북에 연결한 표시 영역. 물리 모니터가 아니라 표시 영역을 가리킨다 — 미러링 상태의 모니터 두 대는 외장 화면 하나다.
_Avoid_: 외장 모니터, 외부 모니터, 외부 화면, 외장 디스플레이, 확장 화면, 보조 화면, 연결된 화면, 세컨 모니터, 서브 모니터
_Why_: '내장↔외장' 반대쌍 유지 + Apple 공식 한국어 용어와 정합. '확장'은 미러링 모드와 모순되고, '보조'는 클램셸 사용자에게 거짓이며, '외부'는 제3자 뉘앙스가 있어 제외했다.

**작업 환경** (workspace):
연결된 외장 화면 식별자의 집합으로 구별하는 저장·복원 단위. A+B와 A 단독은 서로 다른 작업 환경으로 각자의 저장본·저장 대기 이력·닫힌 창 제외 목록을 가지며, 내장 화면은 조합에 포함하지 않는다. 연결 순서·좌우 배치·해상도·포트는 키가 아니다.
_Avoid_: 외장 화면 그룹, 모니터 그룹, 외장 세트, 프로필

**내장 화면** (built-in screen):
맥북 자체의 표시 영역. 창 배치를 저장하거나 창·Space를 옮기지 않는다. 안내형 복원에서 저장된 Space가 어느 화면에 남았는지와 사용자에게 보여줄 화면 이름만 읽을 수 있다.
_Avoid_: 맥북 화면, 노트북 화면, 기본 화면, 메인 화면

**화면 식별자** (screen identity):
외장 화면 하나를 다른 화면과 구별하는 값. 케이블을 다시 꽂아도, 해상도를 바꿔도 같은 값이어야 한다.
_Avoid_: 모니터 ID, 디스플레이 번호, 모니터 이름

**화면 지문** (screen fingerprint):
화면 식별자와 함께 저장하는 하드웨어 특성값(제조사·모델·시리얼). 키가 아니라 검증용이다 — 식별자는 같은데 지문이 다르면 OS가 배정을 바꿨다는 신호이므로 복원하지 않는다.
_Avoid_: 하드웨어 ID, EDID

**화면 표시 이름** (screen label):
사용자에게 보여주는 화면 이름. 모니터 이름에 맥 본체 포트 위치(「왼쪽 위」「오른쪽」)를 붙이고, 위치를 알 수 없거나 같은 이름·같은 위치라 구분이 안 되면 A/B를 붙인다 — 「LG Fine 24 (왼쪽 위)」「LG Fine 24 (오른쪽 · A)」. 표시용이며 저장·복원의 키가 아니다. 포트를 바꿔 꽂아도 표시만 바뀌고 저장본은 그대로다.
_Avoid_: 포트 이름, 화면 번호

**화면 상태** (screen presence):
카드가 보여주는 화면의 세 가지 상태 — **연결됨**(외장 화면이 지금 있다), **기억만**(외장 화면이 없는 동안 아는 화면 하나의 이름과 저장본 유무를 보여준다), **없음**(첫 실행). 상태는 이 셋뿐이며, 분리는 연결됨을 기억만으로 강등할 뿐 지우지 않는다.
_Avoid_: 연결 상태, 접속 상태

**일반 Space** (regular Space):
Mission Control에서 앱 전체화면이 아닌 작업 영역. `Space N`은 한 외장 화면 안의 일반 Space를 현재 순서대로 1부터 센 표시 번호이며(전체화면·Split View 제외), macOS의 전역 `데스크탑 N`과 같다는 뜻이 아니다. 화면마다 1부터 시작한다.
_Avoid_: Screen, 화면, 데스크탑

**잔류 Space** (stranded Space):
외장 화면에 저장했지만 현재는 다른 화면에 속한 일반 Space. Plugback은 읽기 전용 관찰로 출발·목적 화면을 안내할 뿐 직접 옮기지 않는다.
_Avoid_: 잘못된 Space, 유실 Space, 떠난 Space

**개별 Spaces 구성** (separate Spaces per display):
macOS 「각각의 Spaces가 있는 디스플레이」가 켜진 구성. 첫 구현의 지원 조건이다. 꺼져 있거나 확인할 수 없으면 새 Space 기록의 저장·복원을 보류하고 기존 기록과 안내를 유지한다. Space 제약을 지운 위치 복원으로 조용히 전환하지 않는다.
_Avoid_: 공유 Space 모드 지원

### 기록

**창 위치 기록** (window placement):
일반 Space에 속한 대상 앱의 표준 창 하나에 대한 목적 화면·저장된 Space·위치·크기 기록. 같은 Space에 같은 앱의 창이 여러 개 있으면 각 창이 별도 기록을 가진다. 기록마다 저장 자리 identity가 있어 같은 창으로 확인되는 동안 갱신되고, 확인할 수 없으면 새 기록이 생긴다.
_Avoid_: 앱 위치 기록, 대표 창 기록, 프로필 항목

**작업 환경 저장본** (workspace snapshot):
작업 환경 전체의 화면 정보와 창 위치 기록을 한 번에 확정한 값. 수동 저장이든 자동 저장이든 마지막으로 저장이 완료된 것이 그 환경의 복원 소스다. 화면마다 수동·자동을 섞지 않는다.
_Avoid_: 프로필, 슬롯, 레이아웃, 스냅샷(전체 스냅샷과 혼동)
_Why_: 「레이아웃」은 '지금 화면에 펼쳐진 창 배치'라는 일상 용법(버튼 문구 「지금 레이아웃 저장」)에만 허용한다. 저장된 기록물을 레이아웃이라 부르면 전체 스냅샷 제품처럼 들린다.

**저장 대기 이력** (pending history):
작업 중 수집했지만 아직 저장으로 확정하지 않은 상태 기록. 마지막 유효 관찰 하나만 두며, 이력 자체는 복원 기준이 아니다. 수동 저장 뒤 늦게 도착한 옛 관찰과 자동 저장 OFF 전의 관찰은 이력이 되지 않는다.
_Avoid_: 저장본, 복원 소스, 후보

**복원 소스** (restore source):
해당 작업 환경에서 수동·자동 구분 없이 마지막으로 저장이 완료된 저장본. 한 복원 요청은 시작할 때 소스를 고정하며, 수집 중인 이력·다른 환경의 저장본과 섞지 않는다.
_Avoid_: 활성 슬롯, 선택된 슬롯, 기본 슬롯

**앱 선택** (app selection):
작업 환경 단위의 포함·제외. 외장 화면에 나타난 표준 창의 앱은 처음이든 이후든 기본 포함이고, 사용자가 원치 않는 앱만 제외한다. 제외해도 창 위치 기록은 남아 다시 켜면 저장돼 있던 자리로 돌아온다. 카드는 「지금 화면에 있는 앱 / 화면에 없는 저장 앱 / 제외한 앱」 세 묶음으로 보여주며, 저장 전인 앱에는 「저장 전」을 붙인다.
_Avoid_: 대상 등록, 앱 추가

**대상 앱** (target app):
작업 환경에서 제외하지 않은, 창 위치 기록이 있는 앱. 제외한 앱의 창은 어떤 화면에 있든 다루지 않는다.
_Avoid_: 타겟 앱, 등록 앱, 관리 앱

**창 연결** (window link):
저장 자리와 실행 중 실제 창(WindowServer 창 ID)의 대응. 저장·수집 때 관찰한 창과 사용자가 직접 지정해 확정한 창에서 생기며, 메모리에만 있다. 같은 창임을 확인할 수 있는 동안 다음 복원에 먼저 쓰고, 창이 닫히거나 앱이 재실행되면 폐기한다.
_Avoid_: 창 ID 저장, 영구 창 식별자

**창이 닫힌 환경** (window closure context):
저장 자리의 창이 사라진 것을 어느 작업 환경에서 관찰했는가. 같은 환경에서 닫힌 자리는 그 환경의 다음 저장 성공까지 제외하고, 다른 환경·연결 해제 중 닫힌 자리는 유지하며 다시 열기 옵션에 따르고, 닫힌 시점·환경을 모르는 자리는 앱 실행·창 생성을 보류한다.
_Avoid_: 현재 연결 상태, 창 없음

**표준 창** (standard window):
저장·복원이 다루는 유일한 창 종류. 팝업·패널·툴팁·시트는 표준 창이 아니며 존재 자체를 다루지 않는다.
_Avoid_: 일반 창, 메인 창

**비율 좌표** (unit rect):
창 위치 기록이 위치를 기록하는 형식. 픽셀이 아니라 그 화면 크기에 대한 비율이다.
_Avoid_: 상대 좌표, 정규화 좌표, 퍼센트 좌표

**진단 기록** (diagnostics log):
창 확인 실패·잠금 판정 실패·창 생성 실패 사례를 로컬에 최근 100건까지 남긴 기록. 앱·버전, 요청 시작 방식, 창 수, 화면·Space 관찰 상태만 담고 창 제목·탭·문서 내용·화면 캡처·세션 사전 원문은 담지 않는다. 사용자가 내보낼 때만 파일이 되며 자동 전송은 없다.
_Avoid_: 로그 업로드, 원격 진단

### 동작

**저장** (capture):
작업 환경의 작업 상태를 복원에 사용할 저장본으로 확정하는 동작. 사용자가 요청하는 수동 저장과 저장 대기 이력으로부터 만드는 자동 저장은 같은 저장 순서에 놓인다. 파일 기록이 성공한 뒤에만 새 저장본을 공개하며, 실패하면 이전 저장본과 이력을 그대로 둔다.
_Avoid_: 기억, 캡처, 스냅샷 뜨기
_Why_: 화면 상태의 "기억만"처럼 마지막 화면을 기억한다는 서술은 다른 문장이며 허용한다 — 배치를 "기억한다"고 쓰는 것만 피한다.

**수집** (collect):
작업 중 작업 환경의 상태를 저장 대기 이력으로 기록하고 창 연결·닫힘을 갱신하는 단계. 수집만으로 저장본이나 복원 기준이 바뀌지는 않는다. 자동 저장 OFF에서도 창 연결·닫힘 판정은 돈다. 닫힘의 근거는 표준 창 열거가 아니라 존재하는 창 전부의 WindowServer 목록이다 — 다른 Space의 창은 열거에서 빠지지만 닫힌 것이 아니다.
_Avoid_: 자동 저장, 백그라운드 저장, 추적

**수집 트리거** (collect trigger):
수집할 시점을 알려주는 신호원들의 묶음 — 창 이동 정착, 앱 전환, 앱 종료. 활성 Space 변화와 Mission Control 닫힘은 컨트롤러가 먼저 복원 요청의 대기 항목을 다시 판정한 뒤 수집으로 보낸다.
_Avoid_: 감시자, 워처, 리스너

**자동 저장** (auto save):
저장 버튼을 누르지 않아도 작업 환경의 작업 상태를 저장하는 일반 설정(최초 ON). 화면 추가·분리로 작업 환경을 떠날 때와 Plugback을 정상 종료할 때, 마지막 저장본과 내용이 다른 유효 이력을 저장한다. 끄면 저장 대기 이력을 버리고 마지막 저장본은 유지한다. 연결 시 복원을 시작하는 자동 복원과 구별한다.
_Avoid_: 자동 복원, 복원 모드, 자동 슬롯

**확정** (confirm):
유효한 이력의 저장을 성공적으로 완료해 그 작업 환경의 마지막 저장본으로 만드는 단계. 사용자에게 설명할 때는 '저장 완료'를 뜻한다. 같은 내용이면 생략하되, 닫힌 창 제외가 남아 있으면 정리를 위해 저장한다.
_Avoid_: 자동 저장, 커밋, 반영

**복원** (restore):
저장본의 창 위치 기록마다 실제 창을 대응시켜 목적 화면의 비율 좌표로 옮기는 동작.
_Avoid_: 되돌리기, 재배치, 정렬

**복원 요청** (restore request):
연결 자동 복원·「지금 복원」·단축어·「남은 창 복원」 한 번에서 시작해, 고정한 복원 소스의 저장 창마다 결과를 추적하는 단위. 이동·이미 제자리·건너뜀·실패·방문 대기·이동 안내·확인 필요·잠금 보류·취소를 구별한다. 새 요청·새 저장·작업 환경 변경·대상 제외·Plugback 종료로 끝나며, 자동 복원 OFF는 자동으로 시작한 요청의 남은 작업만 취소한다. 재실행은 요청을 이어가지 않는다.
_Avoid_: 복원 세션, 복원 회차, 복원 컨텍스트

**창 대응** (window matching):
저장 자리에 이번에 옮길 실제 창을 정하는 단계. 확인된 창 연결을 먼저 고정하고, 남은 후보는 전체 이동 거리가 가장 짧은 일대일 배정(동률은 크기 차이·일정한 순서)으로 고른다. 후보가 남으면 그대로 둔다. 추정 배정이며 원래 창을 식별한 것이 아니다.
_Avoid_: 창 매칭 보장, 창 동일성 확인

**직접 지정** (direct assignment):
실험실 옵션(최초 OFF). ON이면 구별되지 않는 후보만 「복원 확인」 창에서 사용자가 저장 자리에 연결하고, 「남은 창 복원」으로 적용한다. 확정한 연결은 같은 창·자리를 확인할 수 있는 동안 재사용한다. OFF 전환은 미확정 선택만 취소한다.
_Avoid_: 수동 매칭, 창 고르기 팝업

**다시 열기** (reopen):
실험실 부모 옵션 「종료된 앱 다시 열기」(최초 OFF)와 자식 옵션 「실행 중인 앱 창 되살리기」(실제 창 0개 → 기본 창 하나)·「부족한 창 추가로 열기」(창 하나 이상 → 부족분)의 묶음. 부모 OFF는 앱 실행·명시적 창 생성 전체를 막고 기존 창 배치는 허용한다. 다른 환경·연결 해제 중 닫힌 것으로 확인한 자리에만 적용하며, 실행·생성 직전 최신 설정을 본다. 탭·문서 내용은 복구하지 않는다.
_Avoid_: 앱 자동 실행, 새 창 열기(옵션 이름으로)

**방문 대기** (awaiting visit):
목적 Space가 비활성이어서 사용자가 그 Space를 열 때까지 유효한 요청 안에서 기다리는 상태. 시간 제한과 대기용 반복 타이머가 없으며, Space 전환·Mission Control 닫힘 이벤트에서 다시 판정한다. 카드에 화면 표시 이름과 함께 보이고 「남은 복원 취소」로 끝낼 수 있다.
_Avoid_: pending, 방문 안내(안내 문구 자체와 혼동)

**이동 안내** (move guidance):
저장 Space가 다른 화면에 잔류해 사용자가 Mission Control에서 옮겨야 하는 상태. 방문 대기와 같은 수명 규칙을 따른다.
_Avoid_: Space 자동 이동

**확인 필요** (needs confirmation):
창 대응이 모호하거나(직접 지정 ON), 저장 창이 없는데 닫힌 시점·환경을 알 수 없거나, 저장 Space를 찾을 수 없거나, Space 상태·개별 Spaces 설정을 확인할 수 없어 사용자의 판단이 필요한 상태. 사용자가 「남은 창 복원」을 눌렀을 때만 다시 판정한다 — 늦은 관찰로 자동 재개하지 않는다.
_Avoid_: 대기, 실패

**잠금 보류** (held):
잠금 중이거나 잠금 여부를 판정할 수 없어 아직 OS에 보내지 않은 조작을 멈춘 상태. 잠금 해제가 확인되면 같은 환경의 유효한 남은 항목만 고정한 소스로 재개한다. 이미 보낸 조작은 취소됐다고 가정하지 않는다.
_Avoid_: 잠금 실패

**사용자 조작 보호** (user interaction protection):
자동 복원 요청 시작 뒤 사용자가 직접 옮긴 창을 이번 자동 복원에서 건너뛰는 규칙. 명시적 요청에는 적용하지 않고 영구 제외를 만들지 않는다.
_Avoid_: 창 잠금

**건너뜀** (skip):
복원 대상이지만 창을 그대로 두는 결정. 사유는 일곱 가지다 — 앱이 꺼져 있음, 전체화면, 최소화, 표준 창 없음, 이미 제자리, 이 환경에서 닫음, 사용자가 옮기는 중. 화면 통째 건너뜀(지문 불일치)은 별도의 화면 단위 사유다.
_Avoid_: 무시, 스킵, 패스, 제외

**복원 진행 중** (restoring):
요청의 이동·실행·생성이 실제로 실행되는 구간. 이 구간에는 저장과 재복원이 거부되고, 새 화면 연결은 이 실행이 끝난 직후 새 환경의 요청으로 이어진다.
_Avoid_: 복원 중복, 복원 락

**실험실** (lab):
설정 창의 별도 구획에 있는, 졸업을 약속하지 않은 기능들. 모두 최초 OFF이고 사용자가 바꾼 값은 유지하며 언제든 걷어낼 수 있다.
_Avoid_: 베타, 실험 기능, 고급 설정

**복원 모드** (restore mode):
자동(작업 환경이 바뀌면 그 환경의 저장본으로 즉시 요청 — 기본값)과 수동(버튼·단축어로만 복원)의 두 가지. 자동 저장과는 별개다. 다시 ON으로 켜는 것만으로는 복원하지 않는다.
_Avoid_: 자동 모드 켬/끔, 트리거 설정

**선택 복원** (selective restore):
복원 대상을 대상 앱의 저장 창으로 한정하는 방식. 이 제품이 하는 유일한 복원 방식이다.
_Avoid_: 부분 복원

**전체 스냅샷** (full snapshot):
모든 창의 위치를 한꺼번에 덮어쓰는 방식. 경쟁 제품이 하는 방식이며, 이 제품은 하지 않는다.
