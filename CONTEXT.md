# Plugback

외장 화면을 연결했을 때, 그 화면에 있던 대상 앱의 창만 원래 자리로 복원하는 macOS 메뉴바 유틸리티.
내장 화면의 창과 대상 앱이 아닌 창은 다루지 않는다.

## Language

### 화면

**외장 화면** (external screen):
케이블로 맥북에 연결한 표시 영역. 물리 모니터가 아니라 표시 영역을 가리킨다 — 미러링 상태의 모니터 두 대는 외장 화면 하나다.
_Avoid_: 외장 모니터, 외부 모니터, 외부 화면, 외장 디스플레이, 확장 화면, 보조 화면, 연결된 화면, 세컨 모니터, 서브 모니터
_Why_: '내장↔외장' 반대쌍 유지 + Apple 공식 한국어 용어와 정합. '확장'은 미러링 모드와 모순되고, '보조'는 클램셸 사용자에게 거짓이며, '외부'는 제3자 뉘앙스가 있어 제외했다.

**작업 환경** (workspace):
연결된 외장 화면의 조합으로 구별하는 저장·복원 단위. A+B와 A 단독은 서로 다른 작업 환경으로 각자의 Space별 작업 기록을 가지며, 내장 화면은 조합에 포함하지 않는다.
_Avoid_: 외장 화면 그룹, 모니터 그룹, 외장 세트

**내장 화면** (built-in screen):
맥북 자체의 표시 영역. 창 배치를 저장하거나 창·Space를 옮기지 않는다. 안내형 복원에서 저장된 Space가 어느 화면에 남았는지와 사용자에게 보여줄 화면 이름만 읽을 수 있다.
_Avoid_: 맥북 화면, 노트북 화면, 기본 화면, 메인 화면

**화면 식별자** (screen identity):
외장 화면 하나를 다른 화면과 구별하는 값. 케이블을 다시 꽂아도, 해상도를 바꿔도 같은 값이어야 한다.
_Avoid_: 모니터 ID, 디스플레이 번호, 모니터 이름

**화면 지문** (screen fingerprint):
화면 식별자와 함께 저장하는 하드웨어 특성값(제조사·모델·시리얼). 키가 아니라 검증용이다 — 식별자는 같은데 지문이 다르면 OS가 배정을 바꿨다는 신호이므로 복원하지 않는다.
_Avoid_: 하드웨어 ID, EDID

**화면 상태** (screen presence):
카드가 보여주는 화면의 세 가지 상태 — **연결됨**(외장 화면이 지금 있다), **기억만**(외장 화면이 없는 동안 아는 화면 하나의 이름과 프로필 유무를 보여준다 — 방금 분리됐다면 그 화면, 재시작 직후에는 이름순 첫 프로필의 화면), **없음**(첫 실행). 상태는 이 셋뿐이며, 분리는 연결됨을 기억만으로 강등할 뿐 지우지 않는다.
_Avoid_: 연결 상태, 접속 상태

**일반 Space** (regular Space):
Mission Control에서 앱 전체화면이 아닌 작업 영역. `Space N`은 한 외장 화면에 저장된 일반 Space의 순서이며 macOS의 전역 `데스크탑 N`과 같다는 뜻이 아니다.
_Avoid_: Screen, 화면, 데스크탑

**잔류 Space** (stranded Space):
외장 화면에 저장했지만 현재는 다른 화면에 속한 일반 Space. Plugback은 읽기 전용 관찰로 출발·목적 화면을 안내할 뿐 직접 옮기지 않는다.
_Avoid_: 잘못된 Space, 유실 Space, 떠난 Space

### 프로필

**프로필** (profile):
외장 화면 하나에 속한 대상 앱과 각 앱의 창 위치 기록. 화면 식별자 하나당 **슬롯마다** 프로필 하나.
_Avoid_: 레이아웃, 배치, 설정, 스냅샷
_Why_: 금지는 **프로필의 동의어로 쓰는 경우**에 한한다. '지금 화면에 펼쳐진 창 배치'를 가리키는 일상 용법의 레이아웃은 허용하며, 버튼 문구 「지금 레이아웃 저장」(F-05.2)이 그 용례다 — 지금의 레이아웃을 프로필에 기록하는 동작. 저장된 기록물을 레이아웃이라 부르면 전체 스냅샷 제품처럼 들리므로 그 용법만 피한다.

**슬롯** (slot):
프로필을 수동 기록과 자동 기록으로 구별해 보관하는 자리. **수동 슬롯**(사람이 저장 버튼으로 쓴다)과 **자동 슬롯**(앱이 확정으로 쓴다) 둘뿐이며, 서로의 자리에 쓰지 않는다.
_Avoid_: 프로필 슬롯, 저장 슬롯, 채널, 레이어
_Why_: 두 슬롯이 대등한 저장소라는 인상을 주지 않아야 한다 — 쓰는 주체가 각각 하나로 고정된 것이 이 기능의 안전장치다.

**복원 소스** (restore source):
해당 외장 환경에서 수동·자동 구분 없이 마지막으로 저장이 확정된 기록. 한 복원 요청이 적용할 기준이며, 수집 중인 작업 상태와 구별한다.
_Avoid_: 활성 슬롯, 선택된 슬롯, 기본 슬롯

**저장 대기 이력** (pending history):
작업 중 수집했지만 아직 저장으로 확정하지 않은 상태 기록. 마지막 유효 상태가 자동 저장의 후보이며, 이력 자체는 복원 기준이 아니다.
_Avoid_: 저장본, 복원 소스

**표준 창** (standard window):
저장·복원이 다루는 유일한 창 종류. 팝업·패널·툴팁·시트는 표준 창이 아니며 존재 자체를 다루지 않는다.
_Avoid_: 일반 창, 메인 창

**대상 앱** (target app):
프로필에 등록된 앱. 등록되지 않은 앱의 창은 어떤 화면에 있든 다루지 않는다.
_Avoid_: 타겟 앱, 등록 앱, 관리 앱

**비율 좌표** (unit rect):
프로필이 창 위치를 기록하는 형식. 픽셀이 아니라 그 화면 크기에 대한 비율이다.
_Avoid_: 상대 좌표, 정규화 좌표, 퍼센트 좌표

**창 위치 기록** (window placement):
일반 Space에 속한 대상 앱의 표준 창 하나에 대한 목적 화면·Space·위치·크기 기록. 같은 Space에 같은 앱의 창이 여러 개 있으면 각 창이 별도 기록을 가진다.
_Avoid_: 앱 위치 기록, 대표 창 기록

**Space 작업 기록** (Space workspace record):
한 외장 화면의 일반 Space에 속한 창 목록과 각 창의 위치·크기를 묶은 기록. 탭 URL이나 앱 내부의 작업 내용은 포함하지 않는다.
_Avoid_: Space ID, 창 위치 기록

**창이 닫힌 환경** (window closure context):
창이 실제로 닫힌 시점의 작업 환경과 그 창을 저장한 작업 환경의 관계. 나중에 복원을 요청한 시점의 연결 상태와 구별한다.
_Avoid_: 현재 연결 상태, 창 없음

### 동작

**저장** (capture):
외장 환경의 작업 상태를 복원에 사용할 기록으로 확정하는 동작. 사용자가 요청하는 수동 저장과 저장 대기 이력으로부터 만드는 자동 저장은 같은 저장 순서에 놓인다.
_Avoid_: 기억, 캡처, 스냅샷 뜨기
_Why_: 금지는 **저장 동작의 동의어로 쓰는 경우**에 한한다. 화면 상태의 "기억만"(아래)처럼 마지막 화면을 기억한다는 서술은 다른 문장이며 허용한다 — 배치를 "기억한다"고 쓰는 것만 피한다.

**복원** (restore):
프로필의 비율 좌표대로 대상 앱의 창을 옮기는 동작.
_Avoid_: 되돌리기, 재배치, 정렬

**복원 세션** (restore session):
연결 복원이나 수동 복원 한 번에서 시작해, 그 순간 실제로 발견했고 대상 앱이 묶인 잔류 Space만 끝까지 추적하는 단위. Mission Control 닫힘 뒤 화면 소속을 다시 확인하고, 사용자가 옮긴 Space를 한 번 열면 그 Space의 대상 창 위치를 자동 복원한다. 처음부터 목적 화면에 있던 비활성 Space와 대상 앱이 없는 Space는 세션에 넣지 않는다. 새 복원·저장·대상 변경·프로필 삭제가 일어나면 이전 세션을 버린다.
_Avoid_: 복원 회차, 복원 컨텍스트, 복원 구간

**복원 회차** (restore pass):
대상을 판정하고 옮긴 뒤 결과를 내는 한 번의 복원 실행.
_Avoid_: 복원 세션, 복원 시도

**방문 안내** (visit guidance):
사용자가 잔류 Space를 목적 화면으로 옮긴 뒤, 그 Space를 한 번 열도록 알려주는 복원 세션의 단계. 이 단계가 있는 Space만 방문 뒤 자동 복원을 이어간다.
_Avoid_: 일반 Space 복원, 방문 대기, pending

**Space binding** (space binding):
대상 앱을 저장된 일반 Space에 연결하는 정보. stable snapshot 전체에서 opaque name이 유일할 때만 연결하며, 다른 화면에 하나만 남아 있으면 잔류 Space로 판정한다. 전체화면·Split View·판정 불가는 unresolved로 기록해 창을 움직이지 않는다. 실행 중에만 유효한 Space ID 자체를 뜻하지 않는다.
_Avoid_: Space 연결, Space ID, Space 위치

**Space overlay** (space overlay):
프로필의 창 좌표에 Space binding과 저장된 일반 Space 구성을 덧붙이는 같은 프로세스 안의 기록. 프로필과 같은 복원 소스에서 함께 선택된다.
_Avoid_: Space 프로필, Space 슬롯, Space 레이어

**자동 저장** (auto save):
저장 버튼을 누르지 않아도 외장 환경의 작업 상태를 기록하는 기능. 수집과 확정을 포함하며, 연결 시 복원을 시작하는 자동 복원과 구별한다.
_Avoid_: 자동 복원, 복원 모드

**수집** (collect):
작업 중 외장 환경의 상태를 저장 대기 이력으로 기록하는 단계. 수집만으로 마지막 저장본이나 복원 기준이 바뀌지는 않는다.
_Avoid_: 자동 저장, 백그라운드 저장, 추적
_Why_: 수집은 상태를 알아내는 단계이고 저장은 복원 기준으로 확정하는 단계다.

**수집 트리거** (collect trigger):
수집할 시점을 알려주는 신호원들의 묶음. 창 이동·앱 전환을 쓰지만, 쓰는 쪽은 「수집할 때가 됐다」 하나만 안다. 활성 Space 변화와 Mission Control 닫힘은 컨트롤러가 먼저 안내형 복원을 이어가고, 진행 중 안내가 없을 때만 같은 수집 경로로 보낸다.
_Avoid_: 감시자, 워처, 리스너

**확정** (confirm):
유효한 상태의 저장을 성공적으로 완료해 복원에 쓸 마지막 저장본으로 만드는 단계. 사용자에게 설명할 때는 '저장 완료'를 뜻한다. 이력 수집만으로는 확정되지 않는다.
_Avoid_: 자동 저장, 커밋, 반영

**실험실** (lab):
설정 창의 별도 구획에 있는, 졸업을 약속하지 않은 기능들. 기본 꺼짐이고 언제든 걷어낼 수 있다.
_Avoid_: 베타, 실험 기능, 고급 설정
_Why_: 베타는 "곧 정식이 된다"는 약속을 포함한다. 걷어낼 수 있어야 실험이므로 그 약속을 하지 않는 이름을 쓴다.

**복원 모드** (restore mode):
자동(외장 화면이 연결되면 즉시 복원 — 기본값)과 수동(버튼·단축어로만 복원)의 두 가지. 자동 슬롯의 수집·확정 방식과는 별개다. 일반 Space·전체 화면 복원 범위 설정은 없다.
_Avoid_: 자동 모드 켬/끔, 트리거 설정

**선택 복원** (selective restore):
복원 대상을 대상 앱으로 한정하는 방식. 이 제품이 하는 유일한 복원 방식이다.
_Avoid_: 부분 복원

**전체 스냅샷** (full snapshot):
모든 창의 위치를 한꺼번에 덮어쓰는 방식. 경쟁 제품이 하는 방식이며, 이 제품은 하지 않는다.

**건너뜀** (skip):
복원 대상이지만 창을 그대로 두는 결정. 사유는 다섯 가지다 — 앱이 꺼져 있음, 전체화면, 최소화, 표준 창 없음, 이미 제자리. 화면 통째 건너뜀(지문 불일치)은 별도의 화면 단위 사유다.
_Avoid_: 무시, 스킵, 패스, 제외

**복원 진행 중** (restoring):
복원이 시작되어 아직 끝나지 않은 구간. 이 구간에는 저장과 재복원이 거부되고, 새 화면 연결은 종료 직후의 재복원으로 보류된다.
_Avoid_: 복원 중복, 복원 락

**새 창 열기** (reopen):
실행 중인데 표준 창이 없는 대상 앱에 창을 열게 하고, 창이 실재할 때까지 기다리는 동작. Dock 아이콘 클릭과 같은 신호이되 포커스는 가져오지 않는다.
_Avoid_: 실행, 재실행, 앱 열기, 리오픈
_Why_: 꺼진 앱을 켜는 실행(launch)과 반드시 구별해야 한다 — 이 제품은 앱을 실행시키지 않는다는 원칙이 있고, 새 창 열기는 그 원칙을 깨지 않는 동작이다. '실행' 계열 표현을 쓰면 그 경계가 흐려진다.
